apiVersion: v1
kind: Service
metadata:
  name: registry-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: registry-service
  ports:
    - port: 80
      targetPort: {{ .Values.port }}
  type: ClusterIP
